VITE_BACKEND_URL=http://localhost:8080
VITE_USE_MSW=true
# Solo en prod cross-site (front y back en hosts distintos): URL absoluta del backend.
# En dev dejar sin setear para usar el proxy de Vite ('/api').
# VITE_API_BASE_URL=https://backend-host/api
